meta {
  name: get pending report
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/v1/reports/{{pendingReportId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.status: eq pending
  res.body.outputLanguage: eq en
  res.body.prompts: isUndefined
}
