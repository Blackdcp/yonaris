meta {
  name: docs spec alias is served
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/docs
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.openapi: eq 3.0.3
  res.body.info.title: eq Yonaris API
  res.body.paths: isDefined
}
