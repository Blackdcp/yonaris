meta {
  name: browser runner requires a paired device token
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/internal/browser-runner/v1/tasks/claim
  body: json
  auth: none
}

body:json {
  {
    "brandId": "stepfun"
  }
}

assert {
  res.status: eq 401
  res.body.error: eq BrowserRunnerHttpError
  res.body.message: contains Valid Browser Runner bearer token required
}
