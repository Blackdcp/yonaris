---
"@workspace/config": patch
"@workspace/lib": patch
"@workspace/web": patch
---

Add explicitly started Doubao browser-runner batches, Beijing-time delivery windows, one pre-submit retry, and a consolidated human-resolution queue without changing Elmo metric denominators.
