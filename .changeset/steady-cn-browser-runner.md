---
"@workspace/config": patch
"@workspace/lib": patch
"@workspace/web": patch
---

Add explicitly started Doubao browser-runner batches, Beijing-time delivery windows, dedicated sampling profiles, native-auto search provenance, an isolated browser broker, one pre-submit retry, and a consolidated human-resolution queue without changing Elmo metric denominators.
